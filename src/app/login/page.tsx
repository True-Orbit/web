import { Container } from '@mui/material';
import { LoginForm } from '@/components/forms';

const Login = () => {
  return (
    <Container>
      <LoginForm />
    </Container>
  );
};

export default Login;
