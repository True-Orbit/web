import { Container } from '@mui/material';
import { RegistrationForm } from '@/components/forms';

const Login = () => {
  return (
    <Container>
      <RegistrationForm />
    </Container>
  );
};

export default Login;
